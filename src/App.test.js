import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js';

if('renders without crashing'.()=>{
  coast div = document.createElement('div');
  ReactDOM.render(<App />,div);
});
